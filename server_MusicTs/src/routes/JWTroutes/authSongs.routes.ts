import { Router } from "express";
import { check } from "express-validator";

// controllers

// middlewares

const router = Router()

router.post('/admin/login',)

// router.post('/admin/login',[
//     check('mail','Mail is a must').not().isEmpty(),
//     check('mail','Mail is a must').isEmail(),
// ])

export default router;